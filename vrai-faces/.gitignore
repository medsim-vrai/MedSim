node_modules/
dist/
.vite/
coverage/
playwright-report/
test-results/
*.tsbuildinfo
.DS_Store
.env
.env.local
ios/App/Pods/
android/.gradle/
android/app/build/

# MediaPipe binaries — large/redundant (WASM is an exact node_modules copy; the
# model is a 3.75 MB download). Regenerate with `pnpm run setup:assets`.
# The 12 KB topology JSON and the 46 KB source .obj ARE committed (tiny + reproducible).
packages/core/public/assets/mediapipe/wasm/
packages/core/public/assets/mediapipe/face_landmarker.task
# Kokoro model + voices (~96 MB) — regenerated by `pnpm run setup:assets`.
packages/core/public/assets/kokoro/
