# 2026-06-12

#### Session — FR-004 kit router validation
**Goal**: validate the Beryl AX pocket-router approach before resuming development.
**Deliverables**: leaf cert re-mint for 192.168.8.181 (+old nets+portal.medsim.lan), portal restarted, preflight 6/6 PASS, fresh QR; FR-004 register updated.
**Decisions**: leaf-only re-mint (FORCE guard honored); static-lease + hostname-QR hardening queued.
**Open threads**: tablet-side validation (join SSID, green-lock check, voice take); GL.iNet static lease (user action).
**Tablet half ✅**: both tablets joined Beryl, no new trust step (CA held); Android live take over kit LAN (stt 200 → listen 200) — FR-004 validated AND FR-006b field-validated; user reports Android now responds like the iPad. Remaining: static lease (user), hostname QRs (queued).
**Move-on batch (user away from tablets)**: drug-name hints into room STT (hotwords; A/B: seftriaxone→ceftriaxone fixed), station shows "Pharmacist Lee" not the id, https /onboard route live, chipped binding test un-staled → both gates fully green (126/55). Pushed 97af948 + docs. Portal restarted on the batch. Next live session: hints activate automatically with the med board.
**FR-008 filed**: instructor clarified med-error taxonomy (5 types × 2 vectors × encounter points: report/charting/prep/med-pass) — error-recognition training, instructor arms type+vector+timing. Design sketch in register; detailed plan awaits go-ahead.
**FR-008 amended**: + patient-impact arm (curated consequences, severity tiers, instructor/auto-on-administration trigger, Stabilize revert), + dedicated Setup-stage builder page (6-step bounded wizard), + debrief arc. Plan now S1-S6; awaiting ratification.
**FR-008 S2 shipped**: document vector — SBAR/progress note plants (report/charting), MAR row mutations (prep/med_pass), byte-exact disarm restore, resolve keeps chart for debrief, atomic failed-apply. Found+fixed latent FR-001 bug (active_med_names read "meds" vs ChartSeed "medications" — doctor exclusions were empty). 21 engine tests, portal gate 76/76.
**FR-008 S3 shipped**: verbal vector — staged-order prompt block (deliver once → defend → correct-when-pressed arc, containment rule) doctor-only via _extra_context in both turn paths; delivered-stamping at all reply-log sites; vocab_extras (both sound-alike names) merged into room STT hints. 27 engine tests, portal 82/82. Portal restarted.
**FR-008 S4 shipped**: impact engine — 8 curated consequence profiles × 3 tiers (absolute staged vitals, DRAFT-gated); arm validates impact vs curated mapping (severe+auto pre-confirms, severe+manual double-confirms at trigger); trigger appends vitals.record the EHR renders + patient symptom script via prompt block; stabilize restores captured baseline + retires script; administration hook (cabinet.administer/med.administer) auto-fires matching errors. 34 engine tests, portal 89/89.
**FR-009 filed + researched + planned (paused for review)**: shift-turnover training — deep research ran (22 sources; verification stage hit usage cap → findings flagged for spot-check), plan PLAN-2026-06-12-fr009-shift-handoff.md (H1-H6), PDF strategy report in research/ + Desktop copy. Design: SBAR+I-PASS hybrid checklist, context-pack ground truth, off-going/oncoming modes, verbal survey via room STT, binary coverage + perception-delta scoring. Next actions queued: instructor reviews FR-009 PDF; FR-008 S5 (builder page) + S6 (debrief) remain; then H1.
**FR-009 verification completed + deliverables revised**: 3 workflow passes → 84 adversarial verdicts (77 upheld / 7 refuted / 9 framework claims confirmed 3-0; refutations all hit the one low-tier source never used). New verified source: 2026 scoping review of I-PASS in NURSE handovers → added to report. PDF re-rendered + Desktop copy refreshed; durability pack current (verdicts jsonl/md, run3 output, journal snapshot). Optional closure pass for the upheld-tier after the 1:50pm quota window.
**FR-009 verification COMPLETE**: run 4 post-window → 20/25 confirmed at protocol, 2 refuted (excluded source), 3 quota-stranded closed by direct WebFetch quote-verification. Zero refutations vs used material across 4 passes. Report finalized (quantified perception gap 8.1v7.1, receiver read-back mandate + named tools, training-effectiveness source [11]); PDF re-rendered + Desktop. All artifacts in research/FR-009_research/.
**FR-009 package COMPLETE**: samples built (xlsx 6 sheets incl. standalone authoring template; context-pack + evaluation sample PDFs), coding plan at file grain (H1-H6 + SA1-SA3 dual deployment: handoff_core extraction -> handoff-lite standalone), report finalized w/ §5b dual-deployment + Appendix A; all artifacts on Desktop. Next: instructor ratifies -> FR-008 S5/S6 -> H1.
**FR-008 COMPLETE (S5+S6)**: builder wizard page (/portal/control/errors, 6 steps, bounded), Live staged-errors card (trigger/stabilize/caught/missed/disarm), instructor API w/ transcript stamps, debrief arc section, field script (docs/FR-008-field-script.md). Gates 96 portal / 126 client. Awaiting field validation + catalog clinical review. Portal restarted on S5/S6.
**Bugfix (2026-06-13)**: Medical Record showed the doctor as the patient — seed_from_session used selected_personas[0] blindly + create_session never sets patient_persona_id. Fixed with role-aware patient resolution (roleGroup=="Patient") in ehr_seed.patient_persona_id; routed seed_from_session + register_session primary + med condition + add-device default through it. 4 regression tests, gate 136. Portal restarted (session wiped).
**Bugfixes (2026-06-13, UI)**: (a) Medical Records now opens in a real separate window (window.open medsim_ehr_window) — was a same-tab nav that stranded Back at setup-start. (b) Multi-patient bed now auto-checks the FULL character roster (matches activity.label→same-named sample's personas[]), not just the patient — Hayes brings doctor/charge nurse/RT/wife like single-patient. Client-only, no schema change. 2 roster-contract tests, gate 112. Known gap: 8th activity (resp failure) has no sample + seeds P-006 (pharmacist) as patient — flag for cleanup.
**FR-008 S7 (2026-06-13)**: per-encounter staged errors for multi-patient. Routes resolve ?bed=<encounter_id> (else get_active fallback); engine already keyed by session id. Encounter console gets a staged-error card per bed (build launch + manage chips). Builder threads bed param + shows bed label. Per-bed isolation tested (A doesn't leak to B; wrong-bed lifecycle 404). Gate 101. Portal restarted (session wiped).
**FR-010 filed (2026-06-13)**: post-restart device readiness ping — health-check paired tablets (status/what they run/working?) + warm characters cold→warm (push text + activate mic). P1, investigate-first: needs roster persistence across restart + portal→tablet push channel (reuse speech WS) + browser-gesture wall on mic (likely needs one-tap tablet handshake). Directly addresses the restart-wipes-session friction. The multi-patient med-error request was a verbatim repeat — already shipped FR-008 S7 (9c76baa); per-bed card lives on encounter_console.html.
**FR-008 S7 bugfix (2026-06-13)**: multi-patient 'right med wrong dose' returned no suggestions — bed's med_orders board never initialized (only single-patient meds card does init). Fixed: _ensure_board lazy-inits the board in med_error_routes._resolve for single+multi. S7 fixture had pre-inited the board (masked bug); added regression test that clears it. Gate green. Portal restarted.
**FR-008 field-validated (partial, 2026-06-13)**: live session — system tracked an armed med error and the debrief correctly reported it MISSED. Core arm→resolve(missed)→debrief arc (S1/S5/S6) confirmed in-session. Remaining drills to validate: verbal-vector delivery, document-vector catch, impact trigger/stabilize (FR-008 field script).
**Field-test matrix delivered (2026-06-13)**: docs/field-test/ — 72 tests × 13 core areas. PDF plan (coverage map + Claude issue-pipeline workflow) + Excel (Test Matrix w/ dropdowns, Claude-ingestible Issue Log, live Coverage rollup, Environment inventory). Both on Desktop. Register points at it as the validation instrument. Next: user runs expanded field test, pastes Status=New issues back for fixes; then FR-009.
**FR-009 H1 shipped (2026-06-13)**: portal/handoff.py — context-pack generator. ELEMENTS (11, high-risk flags) + build_pack(session_id, persona_id) composing from ehr_db.seed/fold + med_orders board + med_errors staged-discrepancy probe; severity_for (stable/watcher/unstable via condition trend + abnormal-vital count); pack_vocab (drugs+allergens). Missing-section tolerant (degrades to "(not documented)", never raises). 7 tests. Not yet wired to any route (no behavior change/restart). Next: H2 (AI counterpart prompts + handoff session mode).
**FR-009 H2 shipped (2026-06-13)**: handoff session mode + AI counterpart. handoff.py: start_handoff/end_handoff/state, MODES (offgoing/oncoming) + DIALS (complete/typical_gaps/staged_error; staged_error requires armed FR-008 error), prompt_block_for (counterpart card-id only — receiver-probe block w/ still-unsaid high-risk-first + synthesis ask + transfer + containment; giver block dial-filtered), note_student_utterance (coarse recall-biased element keyword tracker) + still_unsaid, handoff_vocab. Wired: vrai_faces composition hook (both turn sites) + note feed in /listen + room_stt vocab merge. handoff_routes.py (GET/POST start|end, ?bed= for rooms) attached. 10 tests, gate 119. Portal restarted. Next: H3 multi-patient sequencing.
**FR-009 H3 shipped (2026-06-13)**: multi-patient charge-nurse handoff. handoff.py: cap MAX_PATIENTS=3, patient_sources (per-patient pack from its own bed/session), cursor + current_patient/advance_patient (last patient → 'prioritization' phase), expected_priority (severity tier → acuity → order; samples case: unstable post-op > watcher pneumonia > stable CHF), sequence frame in counterpart prompt (patient N of M), prioritization-phase block ("who first and why", no answer given). Routes: patient_sources passthrough + POST /advance. _acuity stored in pack. 7 H3 tests (24 handoff total). Gate 126. Portal restarted. Next: H4 verbal survey on the station.
**FR-009 H4 shipped (2026-06-13)**: verbal survey on the station (FIRST client/TS piece). Portal: handoff.py _SURVEY (6 Qs, mode-filtered: Q1 GAVE/RECEIVED, Q6 first_action oncoming-only) + survey_questions/record_survey_answer/survey_answers; device routes GET /api/face/{cid}/survey + POST .../answer (token posture like /listen, 409 no-handoff). Client: survey_station.ts — createSurveyFlow (testable state machine: load→ready→recording→review→saving→done; press/release/confirm/redo) + mountSurveyStation (DOM, PTT, reuses device_stt portal route); main.ts mounts on ?survey=1. Tests: +3 portal (mode filter, store, routes) + 4 vitest (flow walk, redo, error, no-skip). Client gate 130, portal gate green. Portal restarted. Next: H5 evaluation engine + debrief section.
**FR-009 H5 shipped (2026-06-13)**: evaluation engine — the keystone. portal/handoff_eval.py: score_coverage (BINARY per-element vs pack; deterministic heuristic default + injectable AI scorer; quoted evidence; confirmed=False gate), perception_delta (self-rating digit/word parse 0-10 vs measured %, overestimate/aligned/underestimate; missed-answer "nothing"→blind_spot via _NO_GAP when high-risk misses exist), receiver_metrics (oncoming: questions touched, read-back synthesis, FR-008 discrepancy_caught), auto_prompts (per-miss templates, overestimate prompt leads), build_evaluation/get_evaluation/confirm_coverage (instructor gate). debrief.py: _handoff_section wired into build() (passes session transcript). Smoke: 80% self vs 45% measured→OVERESTIMATE, "Nothing really"→BLIND_SPOT, 5 high-risk misses + prompts. 9 tests, gate 137. Portal restarted. Next: H6 control-room UI + field script (last stage).
**FR-009 H6 shipped — FR-009 COMPLETE (2026-06-13, H1-H6)**: control-room UI + field script. Instructor routes (evaluate/evaluation/confirm) in handoff_routes.py; 🔄 Shift handoff card in control_ops.html (configure mode/dial/counterpart → Start → advance/survey → Score → coverage-confirm panel + perception delta + debrief link); debrief_detail.html handoff section; docs/FR-009-field-script.md (2 drills). All six stages H1-H6 shipped (heuristic scorer; AI hook later). 17 handoff-route tests, gate 139, client 130. Portal restarted. FR-009 awaits field validation; standalone SA1-SA3 future. Build queue now: validation session + clinical reviews; then SA standalone or fleet track.
**GUI redesign delivered (2026-06-13, filed FR-011)**: educator "Mission Control" GUI. Report PDF (critique of attached layered-ecosystem direction + 3 examples [Wizard/Ecosystem Board/Readiness Cockpit] + consolidated "Mission Control" 3-mode shell + resumability) + Excel (comparison/screen-inventory/resumability-state/backlog/readiness-checks) + coding plan PLAN-2026-06-13-gui-mission-control.md (G1-G8, foundation-first: G1 portal resumability [extends ADR-0018 to ~/.medsim SQLite] + G2 readiness API before UI; classic control = fallback). Key insight: ehr_db SQLite already survives restart; persist the in-memory control session there → "Resume" kills restart friction (subsumes FR-010). In docs/gui-redesign/ + Desktop. Pending ratification; G1 = highest-leverage first build.
